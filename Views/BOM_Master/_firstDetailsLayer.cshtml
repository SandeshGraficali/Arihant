<style>


    /* MODAL WRAPPER */
    .modal2 {
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.6);
        z-index: 1000;
        align-items: center;
        justify-content: center;
    }

    /* MODAL CONTENT — EXPANDS BASED ON TABLE SIZE */
    .modal-content2 {
        background: white;
        border-radius: 15px;
        /*width: auto;*/ /* allow natural width */
        max-width: 95vw; /* prevent overflow */
        max-height: 90vh;
        overflow: auto;
        box-shadow: 0 25px 50px rgba(0, 0, 0, 0.4);
        display: inline-block; /* shrink/expand to content */
    }


    .bom-table2 {
        width: 100%;
        border-collapse: collapse;
        margin-top: 20px;
        background: white;
        border-radius: 10px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        /*        overflow:auto;*/
    }

        /* TABLE HEAD */
        .bom-table2 thead {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }

        .bom-table2 th {
            padding: 14px 18px;
            text-align: left;
            font-size: 13px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            font-weight: 600;
            white-space: nowrap; /* keep headers in one line */
        }

        /* TABLE BODY */
        .bom-table2 td {
            padding: 14px 18px;
            border-bottom: 1px solid #f3f4f6;
            font-size: 14px;
            white-space: nowrap; /* allow wide columns */
            overflow: auto;
        }

        /* Hover Effect */
        .bom-table2 tbody tr:hover {
            background: #f9fafb;
        }

    /* STATUS BADGES */
    .badge2 {
        padding: 6px 14px;
        border-radius: 20px;
        font-size: 12px;
        font-weight: 600;
    }

    .badge2-active {
        background: #d1fae5;
        color: #065f46;
    }

    .badge2-inactive {
        background: #fee2e2;
        color: #991b1b;
    }

    /* ACTION BUTTONS */
    .action-btn2 {
        padding: 6px 10px;
        font-size: 12px;
        border-radius: 6px;
        cursor: pointer;
        border: none;
        font-weight: 600;
        transition: 0.3s ease;
    }

        .action-btn2.edit {
            background: #10b981;
            color: white;
        }

            .action-btn2.edit:hover {
                background: #059669;
            }

        .action-btn2.delete {
            background: #ef4444;
            color: white;
        }

            .action-btn2.delete:hover {
                background: #dc2626;
            }

    /* HEADER */
    .modal-header2 {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        padding: 20px 25px;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    /* Close Btn */
    .close-btn {
        background: rgba(255, 255, 255, 0.25);
        border: none;
        color: white;
        font-size: 24px;
        cursor: pointer;
        width: 35px;
        height: 35px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
    }

        .close-btn:hover {
            background: rgba(255, 255, 255, 0.35);
        }
</style>
<div class="modal-content2">
    <div style="width:100%;padding:10px">
        <table class="bom-table2" id="bomtable2">
            <thead>
                <tr>
                    <th><input type="checkbox" id="selectAllBOM"></th>
                    <th>BOM Name</th>
                    <th>Product Type</th>
                    <th>Product Name</th>
                    <th>Product Grade</th>
                    <th>UOM</th>
                    <th>Materials</th>
                    <th>Total Quantity</th>
                    <th>Status</th>
                    <th>Updated Date</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody id="bomTableBodyAll2">
            </tbody>
        </table>

    </div>
</div>

